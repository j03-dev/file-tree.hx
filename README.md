# file-tree.hx

File tree for helix, using Steel.

## Installation

```shell
forge pkg install --git https://github.com/mattwparas/file-tree.hx.git
````

## Usage

```scheme
(require "file-tree-hx/file-tree.scm")

(keymap (buffer FILE-TREE) (with-map FILE-TREE-KEYBINDINGS))
```

